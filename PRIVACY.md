# 收藏汇隐私政策

生效日期：2026 年 9 月 4 日

本隐私政策适用于 Chrome 网上应用店中的“收藏汇”浏览器扩展（扩展 ID：`ijkepampencionfpjjhglogeekgglpnp`）及其配套后端服务。收藏汇用于在用户主动操作时，汇总抖音、小红书和哔哩哔哩收藏页面中当前已经加载的收藏信息，并提供本地管理以及可选的 AI 分类、摘要、标签和语义搜索功能。

## 一、处理的数据

### 1. 本地同步与存储

用户在支持的平台收藏页面主动点击同步后，扩展会从当前页面已经加载的 DOM 内容中读取并在浏览器本地保存以下信息：

- 收藏来源、来源侧内容 ID 和标准内容链接；
- 收藏标题、作者或发布者名称；
- 封面图片地址及部分封面的本地缓存；
- 页面能够提供的有限文本、内容类型、证据等级和同步时间；
- 用户确认的分类目录，以及人工或 AI 生成的摘要、标签和分类结果；
- 整理任务进度和必要的错误状态。

这些信息主要保存在 `chrome.storage.local` 和浏览器 Cache API 中。仅使用本地同步、浏览和关键词筛选功能时，收藏元数据不会发送到收藏汇后端。

### 2. 账户和身份验证信息

用户主动连接收藏汇 AI 服务时，扩展会处理收藏汇应用 Token，并从后端获取和在本地保存账户邮箱、Token 前缀、账户类型、有效期及额度信息。

应用 Token 仅用于向收藏汇后端证明账户权限。扩展不会读取或上传抖音、小红书、哔哩哔哩的密码、Cookie、会话凭据或其他平台登录信息。后端数据库只保存应用 Token 的哈希值，不保存可用于登录的 Token 明文。

### 3. AI 整理和语义搜索

只有当用户已经连接收藏汇 AI 服务，并主动发起 AI 整理或 AI 搜索时，扩展才会通过 HTTPS 向收藏汇后端发送提供该功能所必需的数据。

AI 分类和摘要可能处理：

- 收藏 ID、来源、标题、作者、内容类型；
- 最多 300 个字符的有限来源文本及证据等级；
- 分类目录、分类分配、内容指纹；
- 用户填写的整理偏好。

AI 搜索可能处理：

- 收藏 ID、来源、标题、摘要、标签和最多三级分类路径；
- 用户输入的搜索词、来源筛选条件和结果数量限制。

收藏的标准内容链接、封面图片、完整网页正文和完整音视频不会作为当前 AI 分类、摘要或搜索请求的输入发送给模型服务。

### 4. 服务端记录

为提供账户鉴权、额度管理、请求幂等、防止重复扣费、故障处理和语义搜索，后端可能保存：

- 账户邮箱、账户状态、应用 Token 哈希、有效期和额度使用情况；
- 整理任务 ID、收藏 ID 范围、整理偏好和任务状态；
- 通过结构校验的 AI 分类、摘要或搜索结果；
- 搜索派生文档，包括收藏 ID、来源、标题、摘要、标签和分类路径；
- 模型名称、Token 用量、请求耗时、请求时间和错误代码等运行记录。

生产环境默认不记录完整模型提示词、模型原始响应、应用 Token 明文、平台 Cookie 或平台登录状态。

## 二、数据用途

上述数据仅用于：

- 汇总、保存、展示和管理用户主动同步的收藏；
- 验证收藏汇 AI 服务账户、有效期和可用额度；
- 根据用户请求生成分类、摘要和标签；
- 建立必要的搜索派生索引并完成语义搜索；
- 保证请求幂等、处理故障、保护服务安全和防止滥用。

收藏汇不会将用户数据用于个性化广告、重新定向广告、用户画像、信用评估、贷款决策或与上述单一用途无关的市场研究。

## 三、数据共享和第三方处理

收藏汇不出售、出租用户数据，也不以交换商业利益为目的向第三方提供用户数据。

在用户主动使用 AI 功能时，为生成分类、摘要、标签或搜索结果，必要的数据可能由收藏汇后端发送给当前配置的 AI 模型服务提供商。后端当前支持的模型服务提供商包括：

- OpenAI；
- DeepSeek。

一次请求只会发送给后端当时实际配置的模型服务提供商。相关提供商仅为完成用户请求而处理数据，并受其各自服务条款和隐私规则约束。

收藏汇还可能使用云托管、数据库和网络基础设施服务商来运行后端。这些服务商仅作为基础设施处理者，在提供、保护和维护收藏汇功能所必需的范围内处理数据。除提供功能、保护安全、遵守法律，或发生依法进行的合并、收购或资产转让外，收藏汇不会向其他主体传输用户数据。

## 四、数据保留和删除

- 浏览器本地数据会保留至用户在扩展中删除收藏、清除扩展存储，或卸载扩展。
- 搜索派生文档会保留至下一次全量索引替换、对应文档被删除、账户被删除，或用户提出有效的删除请求。
- 账户、额度、整理任务和请求记录会在账户存续期间，以及为提供幂等、额度核算、安全和故障处理所合理需要的期间保留。
- 法律要求保留的数据可能在适用法律规定的期间内继续保存。

用户可以先在扩展中删除本地收藏或卸载扩展。需要删除服务端账户或相关数据时，可通过本政策末尾的联系方式提出请求。开发者在完成必要的身份核验后，将在合理期限内处理；请勿在公开渠道提交应用 Token、收藏内容或其他敏感信息。

## 五、数据安全

- 扩展与收藏汇后端之间的生产请求使用 HTTPS 加密传输；
- AI 功能使用 Bearer Token 鉴权，并明确禁止在 URL 或日志中记录 Token 明文；
- 后端数据库只保存应用 Token 哈希；
- 扩展将 `chrome.storage.local` 的访问范围限制为受信任的扩展上下文；
- 收藏汇按照最小必要原则限制发送字段、文本长度、列表大小和可访问域名。

任何网络传输或存储都无法保证绝对安全，但收藏汇会采取与数据类型和服务规模相适应的合理安全措施。

## 六、儿童隐私

收藏汇不是面向儿童设计的产品，也不会有意收集儿童的个人信息。如果发现错误收集了此类信息，开发者将在收到通知并完成必要核验后予以删除。

## 七、Chrome 网上应用店 Limited Use 声明

收藏汇对从 Chrome API、网页权限和用户输入获得的信息的使用，遵守 Chrome Web Store User Data Policy（包括 Limited Use 要求）：数据仅用于提供或改进收藏汇明确展示的单一用途及其用户可见功能，不用于出售、个性化广告、信用评估或其他无关用途，也不会允许人工读取用户数据，除非用户就特定数据明确同意、为安全调查所必需、为遵守法律所必需，或数据已经聚合并匿名化且仅用于内部运营。

## 八、政策更新

如果收藏汇的数据处理方式发生实质变化，本政策将同步更新生效日期，并在适用情况下通过扩展界面、Chrome 网上应用店商品详情或其他显著方式通知用户、重新取得必要同意。

## 九、联系我们

如对本政策、服务端数据访问或删除请求有疑问，请通过以下项目支持页面联系维护者：

<https://github.com/lisayaka/favorites-hub-api/issues>

请勿在公开 Issue 中提交应用 Token、账户邮箱、收藏内容或其他敏感信息。维护者会在需要核验身份或处理敏感信息时提供合适的非公开沟通方式。
